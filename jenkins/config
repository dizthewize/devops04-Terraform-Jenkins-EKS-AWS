apiVersion: v1
clusters:
  - cluster:
      server: <elb dns endpoint-url>
      certificate-authority-date: <base64-encode-cert>
    name: kubernetes
context:
  - context:
      cluster: kubernetes
      user: aws
    name: aws
current-context: aws
kind: Config
preferences: {}
users:
- name: aws
  user:
    exec:
      apiVersion: client.authentication.k8s.io/v1alpha1
      command: aws-iam-authenticator
      args:
        - "token"
        - "-i"
        - "myapp-eks-cluster"
